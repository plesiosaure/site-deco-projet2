extends layout
block content

    .container
      .row
        .col-md-12
          .real1title
            .dummy-logo
              h2= articles[0].title
            .detail-description
            p= articles[0].text
    each image in articles
      .imgdetail
        img(src='/img/' + image.thumbnailName, width='1340')


    
