extends layout2

block content
	h1 Editer un article
	form(method="POST" enctype="multipart/form-data")
		.form-group
			label(for='title') Titre de l'article
			input#title.form-control(type='text', value=article.title name='title')
		.form-group
			label(for='text') Texte de l'article
			textarea#text.form-control( name='text')=article.text
		.form-group
			each image in media
				img(src='/img/' + image.thumbnailName height=50)
				label(for="featured"+image.idmedia) Sélectionner comme image principale
				input(id="featured"+image.idmedia type="radio" name="featured" value=image.idmedia checked=image.featured==1)
				label(for="suppr"+image.idmedia) Sélectionner pour supprimer l'image
				input(id="suppr"+image.idmedia type="checkbox" name="suppr[]" value=image.idmedia)
			label(for="images") Ajouter une ou plusieurs images
			input#images(type="file" name="article_sourceName" multiple)


		button.btn.btn-primary(type='submit') Enregistrer
